#include "main.h"

/**
 * _isalpha - check for characters are alpha or not
 * @c: holds the charactor
 * Return: if c is a letter, lowercase or uppercase
 */

int _isalpha(int c)
{
	return (c >= 65 && c <=122);
}
