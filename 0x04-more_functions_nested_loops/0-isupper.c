#include "main.h"
#include <stdio.h>

/**
 * main - checks for uppercase
 *@c: character for check

 * Return: give 1 or 0
 */
int _isupper(int c)
{
if(c >= 'A' && c <= 'Z')
{
return (1);


return (0);
}
