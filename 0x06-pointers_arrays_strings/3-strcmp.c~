#include "main.h"

/**
 *_strcmp - compares two strings
 *@s1: first string
 *@s2: second srting
 *Return: Always 0 (Success)
 */
int _strcmp(char *s1, char *s2)
{
	int x;

	for (x = 0; sl[x] == s2[x]; x++)
	{
		if (s1[x] == '\0')
		{
			return (0);
		}
	}
	return (s1[x] - s2[x]);
}
